# TASKS.md — 야간 실행 체크리스트

- 규칙: 위에서부터 순서대로. 게이트 미통과 시 다음 페이즈 진입 금지
- 상태 표기: `[ ]` 미완료 / `[x]` 완료 / `[S]` SKIPPED(3회 실패) / `[~]` SIMPLIFIED(간이판 대체)

---

## P0. 스캐폴드

- [x] pnpm 모노레포 초기화 (`packages/engine`, `apps/web`), git init, 첫 커밋
- [x] engine: vitest 설정 + 더미 테스트 1개 green
- [x] web: Next.js 15 + R3F 빈 캔버스 렌더 + Playwright 설정, 스크린샷 1장 저장
- 게이트: `pnpm -F engine test` && `pnpm -F web build` 성공

## P1. 장기 룰 엔진 (packages/engine)

- [x] 보드 표현·좌표계·초기 배치 구현 (docs/RULES.md 1~2절)
- [x] 기물별 이동 생성기: 차 (궁성 대각 포함)
- [x] 기물별 이동 생성기: 포 (포다리 규칙 전체)
- [x] 기물별 이동 생성기: 마 (멱 차단)
- [x] 기물별 이동 생성기: 상 (2단계 멱 차단)
- [x] 기물별 이동 생성기: 졸·병 (후퇴 금지, 적 궁성 대각)
- [x] 기물별 이동 생성기: 궁·사 (궁성 내 선 이동)
- [x] 장군 판정 / 자살수 필터링 / 외통 판정
- [x] 한수쉼(패스) 규칙: 장군 상태에서 패스 금지
- [x] 왕 대면(빅장) → 무승부 판정, 동일 국면 3회 반복 → 무승부
- [x] docs/RULES.md 4절 테스트 벡터 전수 구현 + green
- [x] 자체 판단으로 경계 케이스 테스트 20개 이상 추가 작성 + green
- [x] perft 방식 자가 검증: 초기 국면에서 깊이 2 전체 수 생성이 예외 없이 완주
- 게이트: `pnpm -F engine test` 전체 green (실패 0)

## P2. 3D 보드 로컬 대국 (apps/web)

- [x] 3D 장기판: 9x10 교차점, 궁성 대각선, 나무 질감(프로시저럴 머티리얼)
- [x] 기물 렌더링: 팔각 기둥 + 한자 각인(캔버스 텍스처: 楚/漢/車/包/馬/象/士/卒/兵), 초=녹청 / 한=적색
- [x] 입력: 기물 클릭 → engine에서 합법 수 조회 → 도착점 하이라이트 → 클릭 이동
- [x] 로컬 2인 대국 흐름: 턴 표시, 장군 경고 표시, 외통·무승부 시 결과 오버레이
- [x] 부가 UI: 잡힌 말 목록, 패스 버튼, 재시작 버튼, 마지막 수 표시
- [x] 카메라: 기본 부감 + drei OrbitControls (각도 제한)
- [x] E2E: docs/RULES.md 5절의 검증 기보를 Playwright로 자동 재생, 외통 결과 화면까지 도달 + 스크린샷
- [x] (보완) 타이틀 화면 최소 구현: "장기 아레나" + 로컬 대국 버튼 → 대국 화면 전환 (PRD 4절)
- [x] (보완) 모바일 반응형·터치 입력 E2E (390x844 뷰포트, 가로 스크롤 0)
- 게이트: `pnpm -F web build` && `pnpm -F web e2e` green

## P3. Tier 1 공통 포획 연출

- [x] 포획 감지 → 연출 상태머신 진입 (연출 중 입력 잠금)
- [x] docs/SCENES.md 2절 공통 타임라인 구현: 카메라 줌·암전 → 디졸브 발광 → 타격(히트스톱+플래시+파티클) → 재 붕괴 → 복귀
- [x] 디졸브 셰이더(커스텀 ShaderMaterial), 블룸(postprocessing) 적용
- [x] 혈흔 표현: 붉은 파티클 + 바닥 데칼, 설정에서 ON/OFF 토글 (기본 ON)
- [x] 연출 스킵 버튼 (탭/클릭 시 즉시 종료)
- [x] E2E: 포획 발생 기보 재생 → 연출 전·중·후 스크린샷 3장 저장
- [x] (보완) 설정 오버레이 실구현: 혈흔 ON/OFF · 사운드(P6 자리) · 저사양 — PRD 4절에 있으나 P3 목록에 없었음
- [x] (보완) P4 확장 슬롯: 공격 구간(0.8~1.2s)을 기물 타입별로 교체 가능한 attackVariant 레지스트리로 분리
- 게이트: `pnpm -F web e2e` green + 연출 중 프레임 드랍으로 크래시 없음

## P4. Tier 2 기물별 고유 연출 (7종, 각 1개)

- 방침: docs/SCENES.md 3절 사양 기준. 항목이 오래 걸리면 간이판으로 대체하고 `[~]` 표기. 7종 전부 채우는 것이 완성도보다 우선
- [x] 졸·병: 창격 (찌르기 라인 + 스파크) — 정식판: 빛의 장창 3자루 순차 찌르기 후 고정 + 관통 라인
- [x] 차: 돌진 참격 (궤적 잔상 + 슬래시 아크) — 정식판: 잔상 트레일 4단 + X자 슬래시 아크 2개
- [x] 포: 포격 (포물선 발광탄 + 폭발 링 + 연기) — 정식판: 포문 문양 전개 → 포물선 포탄 → 폭발 링 + 연기 기둥
- [x] 마: 도약 강습 (낙하 충격파 + 지면 균열) — 정식판: 도약 + **카메라 상향 추적** + 착지 충격파 + 균열 데칼
- [x] 상: 진각 내려찍기 (거대 그림자 + 방사형 균열) — 정식판: 그림자 확대 상승 → 내려찍기 → 6방향 균열 + 흔들림 강(traumaScale 2.1)
- [x] 사: 호위검 (원형 검막 회전 + 십자 베기) — 정식판: 빛의 검 4자루 회전 검막 → 수렴 → 십자 베기
- [x] 궁 관련: 외통 승리 전체화면 연출 (문양 전개 + 슬로모션 + 승리 문구) — 암전 → 팔괘 문양 전개 → 느린 카메라 선회 → "외통" 붓글씨 → 승자 표기 → 결과 화면. *"슬로모션 최후 일격 리플레이"는 기보 되감기 대신 카메라 선회로 대체*
- [x] (보완) 궁 포획 연출 "왕의 위엄" — 궁도 궁성 안에서는 포획이 가능하다. SCENES.md는 궁에 외통 연출만 배정했으나, 그러면 포획 7종 중 궁만 기본값(전방 돌진)이 되어 게이트를 만족하지 못한다
- [S] 2.5D 캐릭터 실루엣(스트레치): 프리미티브 조립 로우폴리 무사 실루엣이 타격 순간 잔상으로 등장 — **SKIP**. 사유: 7종 연출을 스크린샷 3회 이터레이션으로 이미 확정한 뒤였고, 모든 포획에 0.3초씩 끼어드는 요소라 실패 시 7종 전부의 리스크가 된다. 박스 조립 실루엣이 "무사"로 읽히지 않고 깨진 지오메트리로 보일 확률이 높아 SCENES.md 5절의 "전체 일정을 위협하면 즉시 SKIP" 지침을 따랐다. P6 여력 시 재검토
- 게이트: 7종 기물 모두 포획 시 서로 다른 연출 재생 확인 E2E + 스크린샷

## P5. 온라인 대국 (스트레치)

- [x] apps/server: NestJS + socket.io, 게스트 닉네임, 방 코드 생성·입장 (테스트 51 green, `apps/server/PROTOCOL.md`가 통신 계약)
- [x] 서버 권위 검증: 클라이언트는 (from, to) 좌표만 전송, 서버가 engine으로 합법성·결과 계산 후 브로드캐스트 — 웹은 `game:state` 스냅샷만 그린다 (판을 로컬에서 진행시키지 않는다)
- [x] 턴 타이머 (1수 60초, 초과 시 자동 패스 2회 후 패배), 항복 버튼 — 웹은 `turnDeadline`·`serverTime` 기반 카운트다운 + 자동 패스 누적 경고 + 자체 확인 오버레이(브라우저 confirm 금지)
- [x] 홈 화면: 로컬 대국 / 온라인 방 만들기 / 방 코드 입장 (+ 게스트 닉네임 2~12자, localStorage 유지)
- [x] E2E: Playwright 2개 브라우저 컨텍스트로 온라인 1국 완주 (방 생성→코드 입장→교대 7수→포획 연출 양측 재생→한수쉼→항복 종료)
- [x] (보완) 내 진영 시점 고정 — 한이면 카메라가 판 반대편, 각인 면판도 180° 회전 (`viewSide` prop)
- [x] (보완) 연출 중 수신 스냅샷 큐잉 — 포획 연출이 끝난 뒤 순서대로 반영 (E2E로 검증)
- [x] (보완) 상대 끊김 배너 / 상대 퇴장·기권 결과 처리 / 온라인에서는 재시작 숨김·한수쉼은 `game:pass`
- [x] (보완) 서버 미가동 시 홈 복귀 + 토스트, 로컬 대국 무영향 회귀 테스트 (`?server=` 오버라이드로 죽은 포트 지정)
- 게이트: `pnpm -F server test` 51 green && `pnpm -F web build` && `pnpm -F web e2e` **23개 green** (기존 19 + 온라인 4)

## P6. 마감 (스트레치)

- [x] 사운드: Web Audio 합성 SFX (선택·이동·타격·외통), 음소거 토글 — 합성 전용(외부 파일 0), 연출 비트(소환진·공격 7종 변주·타격·디졸브)까지 rAF로 동기. E2E 3개
- [x] 저사양 모드: 파티클 수 감소 + 블룸 OFF 토글 — 후처리(블룸·비네트) OFF + dpr 1x + 그림자 1024px + **모든 이미터 50%**(공통 366→183, 포 연기 48→24)
- [x] 결과 화면 마감, 타이틀 화면 스타일링 (Pretendard, 전통 문양 배경은 SVG 프로시저럴) — 로고 Pretendard 900 + 잉크→금박 그라디언트, 배경 문양 전량 코드 생성(팔괘·격자·구름), 결과 오버레이를 타이틀과 같은 문법으로 통일 + 무승부 사유 룰 근거 표기
- [x] README.md: 실행 방법, 구조, 스크린샷 첨부 — 스크린샷 5장(타이틀·대국·포획 연출·외통·온라인)
- [x] Docker Compose (web + server) 작성, 빌드 확인 — `docker compose build` 2개 이미지 성공(web 279MB standalone / server 296MB), `up -d` 후 `/health` 200 · 타이틀 200 확인
- 게이트: `pnpm -F engine test` 215 && `pnpm -F server test` 51 && `pnpm -F web build` && `pnpm -F web e2e` **26개 green**

---

## 사용자 미기재 작업 보완 로그

- 오케스트레이터가 발견한 누락 작업은 이 아래에 추가하고 해당 페이즈에도 반영할 것
- (예시 후보: 브라우저 뒤로가기 처리, 모바일 터치 대응, 화면 회전 안내, 접속 끊김 안내)
- [P2] 타이틀 화면 최소 구현 — PRD 4절에는 있으나 TASKS.md P2에 없었음 → P2에 추가·완료
- [P2] 모바일 반응형/터치 E2E — PRD 4절 "모바일에서 깨지지 않는 반응형" 검증 항목이 없었음 → P2에 추가·완료
- [P2] 저사양 토글(P6 항목)의 UI 진입점을 대국 화면 하단에 선반영 (설정 오버레이 자체는 P6)
- [P3] 설정 오버레이(혈흔/사운드/저사양) — PRD 4절 필수 화면인데 P3~P6 어디에도 항목이 없었음 → 혈흔 토글이 P3 필수이므로 P3에서 함께 구현
- [P3] 연출 중 결과 오버레이 억제 — 외통을 만드는 수가 포획이면 연출과 결과 화면이 겹친다. 연출 종료(또는 스킵) 후 표시하도록 처리
- [P3] 바닥 혈흔 데칼의 영속화 + 최대 10개 상한 (SCENES.md는 생성만 명시, 수명 미정의)
- [P4] 궁 포획 연출 — SCENES.md 3절이 궁에 외통 연출만 배정해 포획 시 궁만 기본값으로 남는 구멍이 있었음 → "왕의 위엄" 추가
- [P4] 7종 포획 기보 생성기 — 사·궁은 궁성을 벗어날 수 없어 일반 기보로는 포획이 성립하지 않는다. 한수쉼(합법 액션)으로 상대가 쉬는 동안 적 병을 궁성까지 행군시키는 기보를 설계·엔진 검증 (`pnpm -F web fixtures:p4`)
- [P4] 포획 연출과 승리 연출의 큐잉 — 외통을 만드는 수는 대개 포획이라 두 연출이 겹친다. 포획 연출 종료(또는 스킵) 후 승리 연출이 이어지도록 처리
- [P5] 내 진영 시점 고정 — 온라인은 한 쪽 플레이어가 판 반대편에 앉는다. TASKS.md에 없었지만 시점이 고정되지 않으면 한 플레이어는 계속 뒤집힌 판을 본다 → `viewSide` 도입
- [P5] 연출 중 수신 스냅샷 큐잉 — 서버는 상대 수를 즉시 브로드캐스트하므로 내 포획 연출 중에 다음 국면이 도착한다. 큐에 쌓았다가 연출 종료 후 반영
- [P5] 결과 오버레이 확장 — 서버 판정(기권·시간 초과·자동 한수쉼 몰수)은 엔진 `GameResult`에 없는 종류다. 오버레이가 문구를 직접 받도록 일반화
- [P5] 서버 주소 런타임 오버라이드(`?server=`) — env는 빌드 시점에 박혀 "서버가 꺼진 상태"를 E2E로 재현할 수 없었다 (CLAUDE.md 절대 규칙 7의 회귀 방지용)
- [P6] `SfxDirector` 전역화 — 대국 화면에만 붙어 있어 타이틀·로비 버튼음이 없었고, 오디오 잠금을 푸는 첫 제스처(= "로컬 대국" 클릭)가 유실됐다 → `app/page.tsx` 루트로 이동
- [P6] 저사양 모드가 **변주 이미터를 놓치고 있었다** — 포 연기 기둥(48)이 감소 대상에서 빠져 있었음 → `AttackExtrasProps.lowSpec` 추가로 연결
- [P6] 타이틀 "설정" 버튼 실연결 — PRD 4절이 타이틀 버튼에 설정을 명시하는데 오버레이가 대국 화면에만 있어 `disabled` 상태였다
- [P6] 무승부 사유의 룰 근거 표기 — 결과 오버레이가 "빅장 — 두 궁이 마주 봄"처럼 사유 이름만 말해 규정을 모르는 사람에게 설명이 없었다
- [P6] `.dockerignore` + `output: "standalone"` — Docker 항목에 명시되지 않았으나 둘 없이는 이미지가 pnpm/workspace 심볼릭 링크에 묶여 뜨지 않는다
- [P6] E2E 타이틀 스크린샷의 웹폰트 대기(`waitForFonts`) — 없으면 Pretendard/시스템 폴백 사이에서 스크린샷이 매번 달라진다

---

<!-- 모든 필수 페이즈(P0~P4) 완료 시 아래에 ALL_DONE 기록 -->

ALL_DONE

- P0~P4 필수 페이즈 전부 완료 (2026-08-03 06:10)
- 게이트: `pnpm -F engine test` 215 green · `pnpm -F web build` 성공 · `pnpm -F web e2e` **19개 green**
- 남은 항목은 스트레치(P5 온라인 대국 / P6 마감)와 SKIP 처리된 2.5D 실루엣뿐
- [2026-08-03 06:55] **P5(스트레치) 완료** — 온라인 1:1 대국(서버 + 웹 통합). 게이트 연속 2회: `pnpm -F engine test` 215 · `pnpm -F server test` 51 · `pnpm -F web build` · `pnpm -F web e2e` **23 green**. 남은 항목은 P6 마감과 2.5D 실루엣뿐
- [2026-08-03 07:25] **P6(스트레치) 완료 — 전 페이즈 종료.** 사운드(합성 SFX 전역화)·저사양(이미터 전량 50%)·타이틀 스타일링(Pretendard + 프로시저럴 전통 문양)·결과 화면 통일·README·Docker Compose. 게이트: `pnpm -F engine test` **215** · `pnpm -F server test` **51** · `pnpm -F web build` 성공 · `pnpm -F web e2e` **26 green** · `docker compose build` 성공(+기동 스모크). **미완 항목은 P4의 2.5D 캐릭터 실루엣 `[S]` 하나뿐이며, 이는 SCENES.md 5절 지침에 따른 의도적 SKIP이다**

## P7. 컴퓨터 대국 (사용자 추가 요청, 2026-08-03 아침)

- [x] engine: AI 탐색 모듈 — docs/AI_API.md 계약 구현 (3난이도, 합법성 퍼즈·seed 재현·실력 서열·외통 선택 테스트) — `src/ai/{attacks,eval,search,rng,index}.ts`, 신규 테스트 22개(총 **237 green**), 고급 초기 국면 깊이 4·11.5만 노드/1.2s, 고급 vs 초급 10판 **W8 D2 L0**
- [x] web: 컴퓨터 대국 모드 — 타이틀 진입, 난이도(초급/중급/고급)·진영 선택, Web Worker 탐색, "생각 중" 표시, AI 수에도 동일 연출, E2E + 스크린샷
- 게이트: `pnpm -F engine test` && `pnpm -F web build` && `pnpm -F web e2e` 전체 green

## P8. SD 치비 캐릭터 연출 (사용자 요청, CLAUDE.md 8절)

- [x] 14기물(7종×2진영) 캐릭터 아트 생성 — Higgsfield nano_banana_pro, 여성 9/14(64%), 한자 표기 머리띠/등깃발(背旗) 혼용, 시안 2종 사용자 승인 후 12종 확장, 전수 감사 합격 → `design/chibi/`
- [x] 배경 제거(투명 컷아웃) 후 `apps/web/public/chibi/` 배치 — 14장 `{side}-{type}.webp`(높이 1024px, 투명 배경), 엔진 `PieceType`과 동일 명명이라 런타임에 매핑 테이블이 필요 없다
- [x] 포획 연출 컷인 통합: 타격 순간(1.2s) 공격측 캐릭터 + 피격측 캐릭터 컷인, 로딩 실패 시 기존 연출 폴백, 설정 토글 — `stage.t`를 rAF로 관찰만 하는 부착형(연출 상태머신·스토어 코어 무수정). 0.92s 대각 슬라이드-인 → 1.2s 타격 섬광·흔들림·밀림 → 1.62s 제거(실시간 ≈0.80초 노출), 진영색 스피드라인 밴드, 외통 승리에는 승자 진영 궁이 문양 전개 시점에 크게 등장(붓글씨 비가림 E2E로 고정), 14장 프리로드 + 2중 폴백(실패 시 해당 컷인만 생략·콘솔 워닝), 설정 "캐릭터 컷인" ON/OFF(기본 ON, localStorage), 저사양은 단순 페이드로 축소
- 게이트: `pnpm -F web build` && `pnpm -F web e2e` **34 green**(기존 30 회귀 0 + 신규 4: 타격 컷인 / 스킵 시 즉시 제거 / 외통 승리 컷인 / 토글 OFF) · `pnpm -F engine test` 237 green — 연속 2회 통과
