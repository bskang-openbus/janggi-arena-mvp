# CLAUDE.md — 야간 무감독 오케스트레이터 헌법

## 0. 임무

- 프로젝트명: **Janggi Arena MVP** (한국 전통장기 온라인 게임)
- 목표: 사용자 개입 없이 오늘 밤 안에, 장기 룰을 완전히 지키는 3D/2.5D 웹 장기 게임을 완성한다
- 핵심 검증 가치: "말을 잡는 순간의 전투 연출이 재미를 만드는가"
- 산출물: 실행 가능한 웹 게임 (로컬 2인 대국 필수, 온라인 대국은 스트레치)

## 1. 절대 규칙 (위반 금지)

1. **사용자에게 질문하지 않는다.** 문서에 없는 사항은 스스로 결정하고 `docs/DECISIONS.md`에 기록 후 진행한다
2. **테스트 미통과 상태로 다음 페이즈에 진입하지 않는다.** 각 페이즈의 게이트 조건은 `TASKS.md`에 명시되어 있다
3. **외부 3D 모델·이미지·사운드 에셋을 다운로드하지 않는다.** 모든 지오메트리는 프로시저럴(프리미티브 조합), 모든 사운드는 Web Audio API 합성으로 만든다. **예외(2026-08-03 개정, 8절 참조)**: 전투/피격 연출용 SD 치비 캐릭터 스프라이트에 한해 Higgsfield MCP로 자체 생성한 이미지 자산을 허용한다. 그 외 용도(보드·기물 본체·UI·배경 등)는 여전히 100% 프로시저럴이며, 인터넷에서 찾거나 스크랩한 기성 에셋은 절대 금지(생성 자산만 허용)
4. **허용 목록 외 신규 의존성을 추가하지 않는다** (허용 목록은 4절)
5. **같은 항목 3회 실패 시** 해당 항목을 `SKIPPED`로 표기하고 사유를 기록한 뒤 다음 항목으로 넘어간다. 무한 루프 금지
6. **페이즈 완료마다 반드시 git commit** 한다. 커밋 없는 페이즈 전환 금지
7. 온라인(서버) 기능이 없어도 로컬 대국이 100% 동작해야 한다. 서버 의존이 로컬 모드를 깨면 안 된다
8. 이미 통과한 테스트를 깨뜨리는 변경 금지. 회귀 발생 시 즉시 복구가 최우선
9. 대규모 리팩터링 금지. 동작 우선, 구조 개선은 마지막 P6에서만

## 2. 작업 루프

매 세션 시작 시 다음 순서를 따른다.

1. `TASKS.md`를 읽는다
2. 위에서부터 첫 번째 미완료(`[ ]`) 항목을 찾는다
3. 해당 항목의 관련 문서(`docs/PRD.md`, `docs/RULES.md`, `docs/SCENES.md`)를 읽는다
4. 구현한다
5. 항목에 명시된 검증 명령을 실행하여 통과를 확인한다
6. `TASKS.md`에 `[x]` 체크하고, 결정사항이 있으면 `docs/DECISIONS.md`에 기록한다
7. `git add -A && git commit -m "<페이즈>: <항목 요약>"`
8. 다음 항목으로 진행한다

- 모든 필수(P0~P4) 항목이 체크되면 `TASKS.md` 최하단에 `ALL_DONE`을 기록한다
- 스트레치(P5~P6)는 필수 완료 후 시간이 남을 때만 진행한다

## 3. 자율 결정 권한

다음은 명시적으로 위임된 결정 권한이다. 판단이 필요하면 즉시 결정하고 기록한다.

- 문서에 정의되지 않은 UI 세부 배치·색상·문구
- 이펙트 파라미터(파티클 수, 지속 시간, 색상)의 미세 조정
- 코드 구조·파일 분할·네이밍
- 성능 문제 발생 시 이펙트 강도 하향
- 문서 간 충돌 발견 시: RULES.md > TASKS.md > PRD.md > SCENES.md 우선순위로 해석
- **사용자가 빠뜨린 작업 발견 시: 스스로 목록화하여 TASKS.md의 적절한 페이즈에 추가하고 구현한다** (예: 턴 표시 UI, 잡힌 말 목록, 무르기 여부, 게임 재시작 버튼 등)

## 4. 기술 스택 (고정, 변경 금지)

```text
모노레포: pnpm workspace
├─ packages/engine     장기 룰 엔진 (순수 TypeScript, 의존성 0, 프론트·서버 공유)
├─ apps/web            Next.js 15 (App Router) + TypeScript + Tailwind CSS v4
│    ├─ three, @react-three/fiber, @react-three/drei
│    ├─ @react-three/postprocessing
│    └─ zustand
└─ apps/server         NestJS + socket.io (P5 스트레치에서만 착수)

테스트: vitest (engine 단위테스트), Playwright (web E2E)
폰트: Pretendard (CDN 허용, 외부 리소스 예외)
캐릭터 에셋: 전투/피격 연출 전용 SD 치비 스프라이트 — Higgsfield MCP 생성, 저장소에 정적 파일로 커밋 (런타임 외부 호출 없음). 8절 참조
```

- 허용 의존성: 위 목록 + 각 프레임워크의 필수 피어 의존성만
- Node 20+, ESM 기준

## 5. 검증 명령 표준

```bash
pnpm -F engine test        # 룰 엔진 단위테스트 (P1 게이트)
pnpm -F web build          # 프론트 빌드 성공 (P2~P4 게이트)
pnpm -F web e2e            # Playwright 시나리오 (P2~P4 게이트)
pnpm -F server test        # 서버 테스트 (P5 게이트)
```

- Playwright 검증은 headless로 실행하고, 각 시나리오의 스크린샷을 `artifacts/` 에 저장한다 (아침 확인용)

## 6. 진행 기록

- `TASKS.md`: 체크리스트 상태 (진실의 원천)
- `docs/DECISIONS.md`: 자율 결정 로그 (시각, 결정, 사유)
- `PROGRESS.md`: 페이즈 완료 시각과 한 줄 요약을 append (아침 브리핑용)
- git 히스토리: 페이즈·항목 단위 커밋

## 7. 시간 관리

- 우선순위는 TASKS.md의 순서 그 자체다. 앞 페이즈가 무너지면 뒤 페이즈를 버린다
- P4(기물별 고유 연출 7종)는 항목당 타임박스 개념으로 접근한다. 고유 연출이 오래 걸리면 SCENES.md의 "간이판"으로 대체 구현하고 `SIMPLIFIED` 표기 후 전진한다. 완성도보다 7종 전부 채우는 것이 우선이다
- 아침 기준 최소 성공선: P0~P3 완료 (룰 완전 + 3D 대국 + 공통 포획 연출)

## 8. SD 치비 캐릭터 에셋 정책 (2026-08-03 헌법 개정, P8)

- 배경: 1절 3항의 "전(全) 프로시저럴/합성" 원칙은 P0~P7까지 그대로 지켜졌다. 사용자가 전투·피격 연출에 SD 치비 캐릭터를 추가해 달라고 명시적으로 요청했고, 이는 AI 생성 이미지 자산 도입을 뜻하므로 오케스트레이터가 임의로 결정하지 않고 사용자에게 확인 후 본 절로 헌법을 개정한다
- **허용 범위(정확히 이만큼)**: 포획 연출·피격 리액션 컷인에 쓰이는 SD 치비 캐릭터 스프라이트(정지 이미지/스프라이트시트)만. 보드·기물 본체(팔각기둥)·UI·배경·문양은 전부 기존 그대로 프로시저럴 유지 — 기물을 치비 캐릭터로 통째로 교체하는 것이 아니라, 연출 클로즈업 순간에만 등장하는 컷인 레이어다
- **생성 도구**: Higgsfield MCP만 사용. `get_workflow_instructions(character-sheet)`로 진영별(초/한) 스타일 포뮬러를 먼저 고정 → `generate_image`로 포즈 시트 생성 → `remove_background`로 투명 컷아웃. 인터넷에서 찾거나 스크랩한 기성 이미지·타사 IP·저작권 소지가 있는 리소스는 절대 금지 — **오직 이 세션에서 생성한 자산만** 허용
- **자산 관리**: 생성된 스프라이트는 저장소에 정적 파일(`apps/web/public/chibi/` 등)로 커밋하고 런타임에는 외부 네트워크 호출 없이 로컬 파일로만 서빙한다. 생성 과정(프롬프트·모델·잡 ID)은 `docs/DECISIONS.md`에 기록
- **범위 축소 원칙**: 7기물×2진영=14종 전부에 각각 다른 캐릭터를 만들지 않는다. 진영별 1~2 원형(archetype, 예: 보병형/장수형)에 무기·색만 기물 타입별로 다르게 하여 소수 캐릭터로 커버한다 (SCENES.md 5절의 기존 "무기 유형별 차별화는 여력 시에만" 방침 계승)
- **테스트 게이트 불변**: 이 개정은 1절 2·8절(테스트 게이트·회귀 금지)을 면제하지 않는다. 새 자산 로딩 실패 시 기존 연출(파티클/디졸브)로 폴백해야 하며, 자산 부재가 E2E를 깨뜨리면 안 된다
- 이 정책은 P8(컴퓨터 대국 다음 페이즈)로 TASKS.md에 편성하고 진행한다
