# PROGRESS.md — 야간 빌드 진행 기록 (아침 브리핑용)

- 형식: `[완료 시각] 페이즈 — 한 줄 요약`

---

- [2026-08-03 02:56] P0 완료 — pnpm 모노레포(engine+web) 스캐폴드, engine vitest 더미 테스트 green, web에 R3F 캔버스 렌더 + Playwright E2E green, artifacts/p0-canvas.png 저장. 게이트 3종(`engine test`/`web build`/`web e2e`) 전부 통과.
- [2026-08-03 03:32] P1 완료 — 장기 룰 엔진(packages/engine, 런타임 의존성 0/ESM/strict) 구현. vitest **103개 전부 green**(RULES.md V01~V30 전수 + 자체 경계 케이스 73개). perft 초기 국면 **depth1=32(수 31 + 한수쉼), depth2=1024** 예외 없이 완주. E2E 기보 2종 생성·검증(`capture-game.json` 7수·6번째 수에서 초 차가 한 병 포획 / `mate-game.json` 9수 초 승 외통). 게이트 `pnpm -F engine test`·`pnpm -F web build` 통과.
- [2026-08-03 03:52] P2 완료 — 엔진↔3D 보드 통합으로 로컬 2인 대국 완성. zustand 스토어(엔진 단일 진실원) + 어댑터 레이어로 시각 컴포넌트 무수정 소비, 타이틀→대국 화면 전환, 턴/장군 배너/잡힌 말(전과)/마지막 수/한수쉼(장군 시 비활성)/재시작/결과 오버레이, 모바일 터치 대응. 데모 스텁 제거. Playwright **12개 전부 green**(포획 기보 7수→兵 전과 표시, 외통 기보 9수→"초 승" 오버레이, 장군 중 패스 차단, 모바일 390px 가로 스크롤 0). 스크린샷 7장 artifacts/에 저장. 게이트 3종(`engine test` 103 green / `web build` / `web e2e`) 전부 통과.
- [2026-08-03 04:35] P3 완료 — Tier1 공통 포획 연출(2.9초) 구현. 상태머신(zustand 위상 + 가변 stage 싱글턴, 프레임 우선순위 −2), SCENES.md 2절 타임라인 전량(암전·측면 줌 카메라 → 팔괘 소환진 → 전방 돌진 → 히트스톱 100ms+붉은 플래시+충격파 2겹+스파크/혈흔/재 366파티클+트라우마 흔들림 → 커스텀 ShaderMaterial 디졸브(주홍 경계 발광) → 복귀), 혈흔 ON/OFF 설정 오버레이(OFF 시 백금색 대체·데칼 미생성), 연출 스킵(화면 탭/버튼), 바닥 혈흔 데칼 영속화. **P4 확장 슬롯**: 0.8~1.2s 공격 구간을 `attackVariant` 레지스트리로 분리(현재 `lunge` 1종, P4가 7종을 꽂음). Playwright **16개 전부 green**(기존 12 + 신규 4), engine 215개 회귀 없음. 스크린샷 자가 평가 3회 이터레이션(피격 기물 가림 → 타격 간격 0.64유닛, 각인 흰 뭉개짐 → 발광 상한 1.0, 카메라 고도 38°+듀얼 키라이트). artifacts/p3-{before,during,dissolve,after,nogore}.png 저장.
- [2026-08-03 06:10] P4 완료 — Tier 2 기물별 고유 연출 7종 + 외통 승리 연출. P3의 `attackVariant` 슬롯에 졸(창격·빛의 장창 3자루)·차(돌진 참격·잔상+X자 아크)·포(포격·포문 문양+포물선 포탄+연기 기둥)·마(도약 강습·카메라 상향 추적+착지 균열)·상(진각 내려찍기·6방향 균열+흔들림 2.1배)·사(호위검·회전 검막 4자루→십자 베기)·궁(왕의 위엄·심판 문양 강하+광휘 기둥)을 등록. 전부 SCENES.md 3절 **정식판**. 외통 승리 연출(암전→팔괘 문양→느린 카메라 선회→"외통" 붓글씨→승자 표기→결과 화면)과 포획↔승리 연출 큐잉 구현. 슬롯은 `traumaScale`·`cameraLift` 두 개만 확장. 사·궁 포획 기보는 한수쉼을 이용해 적 병을 궁성까지 행군시키는 방식으로 설계하고 엔진 전수 검증(`pnpm -F web fixtures:p4`, 7/7). Playwright **19개 green**(기존 16 + 신규 3), engine 215 green. 스크린샷 자가 평가 3회 이터레이션(타격 플래시가 시그니처를 덮음→캡처 시각 이동, 포탄·마가 프레임 이탈→궤도 하향+카메라 추적, 장창이 1자루로 보임→찌른 뒤 고정+측면 분산). artifacts/p4-{soldier,chariot,cannon,horse,elephant,guard,general}.png + p4-mate.png 저장. **P0~P4 필수 페이즈 전부 완료 → TASKS.md에 ALL_DONE 기록.**
- [2026-08-03 06:55] P5 완료 — 온라인 1:1 대국(웹 통합). socket.io-client 도입, 타이틀 3버튼(로컬/방 만들기/방 코드 입장) + 게스트 닉네임(2~12자, localStorage) + 대기실(방 코드 표시), 서버 권위 흐름(클라이언트는 from/to·pass·resign만 전송하고 `game:state` 스냅샷을 그대로 렌더), 내 진영 시점 고정(한이면 카메라 반대편 + 각인 180°), 내 턴에만 입력, 포획 시 P3·P4 연출을 양측에서 재생하고 **연출 중 도착한 스냅샷은 큐잉 후 반영**, 턴 시계(`turnDeadline`−`serverTime` 보정 카운트다운)·자동 한수쉼 누적 경고, 항복(자체 확인 오버레이·브라우저 confirm 미사용), 상대 끊김 배너, 온라인에서는 한수쉼=`game:pass`·재시작 숨김·방 나가기. 서버 미가동 시 홈 복귀+토스트이며 **로컬 대국은 무영향**(회귀 테스트 포함). Playwright **23개 green**(기존 19 회귀 없음 + 온라인 4: 1국 완주/연출 큐잉/상대 끊김/연결 실패), server 51 green, engine 215 green. artifacts/p5-online-{1,2,2-han,capture,3}.png 저장. E2E는 webServer 2개(next dev + node dist/main.js, 서버 포트 3003 — 3001은 타 프로젝트 점유)로 자동 기동.
- [2026-08-03 07:25] P6 완료 (전 페이즈 종료) — 마감 5종. **사운드**: 별도 작업으로 머지된 Web Audio 합성 SFX(`e09c2f8`→`6e22a0d`, 외부 음원 0)의 `SfxDirector`를 대국 화면에서 **앱 루트로 올려 전역화** — 타이틀·로비 버튼음이 나지 않던 문제와, 오디오 잠금을 푸는 첫 제스처(="로컬 대국" 클릭)가 통째로 유실되던 문제를 함께 해소. **저사양 모드**: 후처리 OFF + dpr 1x + 그림자 1024px에 더해 **모든 파티클 이미터를 50%로**(공통 366→183, 누락돼 있던 포 연기 기둥 48→24까지 `AttackExtrasProps.lowSpec`으로 연결), 설정 문구를 실제 동작대로 정리. **타이틀 스타일링**: Pretendard(CDN, 유일 예외) 900 weight + 자간 + 잉크→금박 그라디언트 + 2단 drop-shadow로 붓글씨 감성 구현(서예 폰트 없이), 배경은 전량 코드 생성 SVG(`TraditionalPattern.tsx` — 팔괘 3획 비트마스크, 장기판 9×10 격자 + 궁성 X자, 반원 스캘럽 + 아르키메데스 나선 구름), 진영색 좌초/우한 그라디언트 + 중앙 radial mask, 버튼 4개를 한자 배지 + 설명 2줄 구조로 정돈하고 **타이틀 설정 버튼을 실제로 연결**. 스크린샷 자가 평가 2회(1차: 팔괘 r=392가 로고·문구와 정면 충돌, 격자 안 보임, 구름 과함 → 2차: r=520으로 위·아래 괘를 화면 밖으로 빼고 격자 진하게/구름 옅게). **결과 화면**: 로컬·온라인 공용 오버레이를 타이틀과 같은 문법(선–한자 `對局終了`–선 + 승패색 강조선)으로 통일하고 무승부 사유에 룰 근거 한 줄 추가. **README.md**: 소개·스크린샷 5장·실행(로컬/온라인/Docker)·모노레포 구조·테스트 4종·기술 스택·룰 구현 범위·조작법·문서 색인. **Docker**: `apps/web/Dockerfile`(Next standalone 3스테이지) + `apps/server/Dockerfile`(engine dist 포함 멀티스테이지, 런타임 --prod 재설치) + `docker-compose.yml`(web 3002 / server 3001, `NEXT_PUBLIC_SERVER_URL` build arg ↔ `CORS_ORIGIN` 연결, healthcheck) + `.dockerignore`. 게이트 전부 green: engine **215** · server **51** · `web build` · Playwright **26 green**(회귀 0) · `docker compose build` 2개 이미지 성공(web 279MB / server 296MB) + `up -d` 스모크(`/health` 200, 타이틀 HTML 200) 후 정리. **미완 항목은 P4의 2.5D 캐릭터 실루엣 `[S]` 하나뿐(의도적 SKIP).**
- [2026-08-03 11:40] P7 완료 — 컴퓨터 대국 (AI 3난이도: 初 얕은 탐색+무작위 / 中 알파베타 d3 / 高 반복심화+TT+quiescence 1.2s). 엔진 AI 테스트 22개(서열 대국 고급 8승2무0패 vs 초급), 웹 워커 통합, E2E 30 green. 게이트 전부 통과.
